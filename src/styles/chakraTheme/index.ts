import { extendTheme } from '@chakra-ui/react';

export const chakraTheme = extendTheme({
  colors: {
    customColor: {
      300: '#A181DF',
      400: '#A165DF',
      500: '#A53EDF',
    },
  },
});
